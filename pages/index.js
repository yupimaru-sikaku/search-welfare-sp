import Layout from "../Components/Layout";
import Auth from "../Components/Auth";

const Home = () => {
  return (
    <Layout title={"認証画面"}>
      <Auth />
    </Layout>
  );
};

export default Home;
